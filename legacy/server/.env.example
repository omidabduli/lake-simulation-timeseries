# Settings for the LEGACY Hetzner/CloudPanel deployment (legacy/server/deploy.command).
# Copy to the repository root as `.env` and fill in the values. `.env` is git-ignored;
# never commit real values. Not needed for the GitHub Pages deployment.
SERVER_IP=
SITE_USER=
SITE_PASSWORD=
SITE_DOMAIN=
SITE_PORT=
SSH_PORT=22
