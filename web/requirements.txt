# Python packages for the in-browser (Pyodide) runtime of the GitHub Pages build.
#
# Packages that ship with Pyodide are used at the version bundled with the
# pinned stlite release (see STLITE_VERSION in web/index.html): currently
# numpy 2.2.5, pandas 2.3.3, scikit-learn 1.7.0, xgboost 2.1.4, xlrd 2.0.1.
# Pure-Python packages are installed from PyPI and pinned here.
#
# `shap` is intentionally absent: it needs numba, which cannot run in
# WebAssembly. modules/explainer.py then uses XGBoost's built-in TreeSHAP,
# which returns identical values.
numpy
pandas
scikit-learn
xgboost
xlrd
openpyxl==3.1.5
plotly==6.9.0
